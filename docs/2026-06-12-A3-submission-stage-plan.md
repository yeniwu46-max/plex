# PLEX A3 提交与答辩可信度阶段计划

**周期：2026-06-12 至 2026-06-18**

## 阶段目标

把当前可运行系统转换为可审计、可复现、可答辩的候选提交版本。停止新增普通业务
页面，优先处理证据、交付物、告警、外部验收和演示稳定性。

## 任务顺序

1. **提交审计自动化**：生成依赖许可证、证据完整性和发布就绪 JSON；本地与外部
   闸门分开计算。
2. **正式文档收口**：维护 `docs/submission/01-10`，所有指标链接机器报告，删除
   “规划中”与“已完成”混写。
3. **工程债务收敛**：优先替换关键路径 `Query.get()` 和 `datetime.utcnow()`，
   将全量测试告警降到可解释范围。
4. **真实环境插入验收**：取得讯飞凭证后执行画像、资源、驿站三项冒烟；GitHub
   可用后归档 Python 3.12、MySQL 8、Node 20 Job 链接。
5. **候选提交包**：生成排除密钥、数据库、缓存和开发产物的文件清单、版本号、
   Git 提交号与 SHA-256。
6. **答辩材料**：从 JSON 证据生成图表，完成 PPT、七分钟脚本、主备路线和两轮录像。

## 闸门

- G4-L：本地测试、构建、安全、性能、知识库、干净环境、依赖和文档审计全部通过。
- G4-E：真实讯飞、在线 CI、MySQL 8 均有可归档证据。
- G5：候选包在干净目录可运行，PPT、视频、文档和系统状态完全一致。

截至 2026-06-12，G4-L 正在执行；G4-E 因凭证和外部环境保持阻塞。TTS 未实现，
不作为当前主线能力。
