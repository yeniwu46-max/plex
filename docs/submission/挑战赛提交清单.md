# 挑战赛提交清单与打包说明

**截止**：2026-08-01（周六）09:00  
**收件**：cnsoftbei@qq.com  
**方式**：个人百度网盘分享**文件夹**（可在线打开，不要传压缩包）→ 加密链接 + 提取码写在邮件正文

## 一、提交文件夹建议结构

```text
24011308-挑战赛/
├── README.md                          # 启动说明 + 开源协议表
├── 一键启动.bat
├── source/                            # 可运行源码（frontend + backend）
├── data/                              # 知识库 + 演示库
├── runtime/                           # 预构建前端等（如有）
├── config-samples/                    # 环境变量样例（无真实密钥）
├── docs/
│   ├── 配套文档/                      # 需求/设计/多智能体/手册/创新点
│   ├── AI-coding使用说明.md
│   └── 挑战赛理由及作品亮点.docx      # 必交
├── demo/
│   ├── PLEX-A3-defense-challenge.pptx # 答辩 PPT
│   ├── 演示视频.mp4                   # ≤7 分钟（需团队录制）
│   └── 报名表.docx                   # 挑战赛报名表（组委会模板）
└── reports/                           # 评测/安全/依赖清单
```

## 二、已在仓库就绪的材料

| 材料 | 路径 |
| --- | --- |
| 挑战赛理由 Word | `docs/submission/artifacts/挑战赛理由及作品亮点.docx` |
| 挑战赛答辩 PPT | `docs/submission/artifacts/PLEX-A3-defense-challenge.pptx` |
| 演示脚本 | `docs/submission/10-demo-script.md` |
| 初赛成片（可参考或重录） | `docs/submission/artifacts/PLEX-A3-supporting-documents-20260720/演示视频.mp4` |
| 初赛 PPT（可参考） | `.../plex演示PPT.pptx` |
| 需求/创新/手册 | `docs/submission/artifacts/.../配套文档/markdown/` |

## 三、你还需要手动完成的 3 件事

1. **按 `10-demo-script.md` 重录 ≤7 分钟演示视频**（务必覆盖：画像流式、时间线、语音/视频卡片、驿站流式）。把 MP4 放进提交文件夹的 `demo/`。
2. **填写组委会《挑战赛报名表》**，与《理由及亮点》一并放入文件夹。
3. **上传百度网盘分享文件夹**（可在线打开、加密），邮件正文只写链接+提取码，**8月1日 09:00 前**发到 cnsoftbei@qq.com。

## 四、打包前自检

```bat
cd zhongruan
start.bat --check
cd backend
..\.venv\Scripts\python.exe -m unittest discover -s tests -q
```

确认：

- [ ] 无 `.env` 真实密钥进入提交包（只用 config-samples）
- [ ] PPT 与 MP4 实体文件在文件夹内（不是“待生成”）
- [ ] README 有开源协议表
- [ ] 挑战赛理由 Word 已放入
- [ ] 一键启动可跑通演示账号

## 五、可选：填入多模态密钥提升演示效果

写入 `backend/.env`（不要提交到网盘）：

```
IFLYTEK_TTS_APP_ID=
IFLYTEK_TTS_API_KEY=
IFLYTEK_TTS_API_SECRET=
ARK_API_KEY=
ARK_VIDEO_MODEL=
ARK_IMAGE_MODEL=
```

未填写时：语音走本地 SAPI 兜底；视频展示分镜脚本；图解跳过——主流程仍可演示。
